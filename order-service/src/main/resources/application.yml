server:
  port: 8083

spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mariadb://mariadb:3306/geunuk_order?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ${DB_USERNAME:geunuk}
    password: ${DB_PASSWORD:geunuk1234}
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://mongodb:27017/geunuk_logs}

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
